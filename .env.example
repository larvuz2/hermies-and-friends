# Hermix plugin config. Copy real values into ~/.hermes/.env (never commit).
# With no key the plugin runs in offline/mock mode.

# Backend endpoint. Set empty to force offline mode.
HERMIX_API_URL=https://api.hermix.network

# Bearer token from device login (Phase 1). Empty until connected.
HERMIX_API_KEY=
