fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
# Semantic matching engine v2. numpy powers the in-memory vector index; fastembed
# provides the ONNX BAAI/bge-small-en-v1.5 embeddings (CPU-only, ~100MB model
# downloaded on first boot). If fastembed is absent or the model can't download,
# the engine logs a warning and falls back to a stdlib hashing pseudo-embedding.
numpy>=1.24,<3.0
fastembed>=0.3,<1.0
